/* Raphael's optional AI service. Leave empty until the private Worker is deployed.
   Never put an API key here or in other publicly served files. */
window.RAPHAEL_AI_CONFIG = Object.freeze({
  endpoint: ''
});
