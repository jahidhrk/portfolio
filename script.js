const reveals=document.querySelectorAll('.reveal');const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')}),{threshold:.12});reveals.forEach(el=>io.observe(el));const glow=document.querySelector('.cursor-glow');window.addEventListener('pointermove',e=>{glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'});const line=document.querySelector('.timeline .line i');window.addEventListener('scroll',()=>{if(line){const t=document.querySelector('.timeline').getBoundingClientRect();const p=Math.max(0,Math.min(1,(innerHeight-t.top)/(t.height+innerHeight*.25)));line.style.height=(p*100)+'%'}const orbit=document.querySelector('.orbit');if(orbit)orbit.style.transform=`translateY(${scrollY*.08}px) rotate(${scrollY*.015}deg)`});document.querySelectorAll('.work-card').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateX(${-y*2.5}deg) rotateY(${x*2.5}deg) translateY(-6px)`});card.addEventListener('pointerleave',()=>card.style.transform='')});